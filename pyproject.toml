[project]
name = "sentinel-mcp-server"
version = "0.1.0"
description = "Simple security scanning MCP server for Dedalus Labs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
